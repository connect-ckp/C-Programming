#include <stdio.h>

int main(void) {
	/****************************************************************
	 * Null Character: The charater \0 is used to terminate a String.
	 * It is added by default to the end of a String.
	 ****************************************************************/
	printf("The character \0 is used to terminate a string."); 
	//Output: The character 
	return 0;
}